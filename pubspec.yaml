name: sky_ui_kit
description: A package contains some common widget for faster development.
version: 0.0.2
repository: https://github.com/nandakista/sky_ui_kit

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  chewie: ^1.3.5
  video_player: ^2.4.7
  flutter_svg: ^1.1.0
  cached_network_image: ^3.2.1
  carousel_slider: ^4.1.1
  path: ^1.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

  assets:
    - assets/
